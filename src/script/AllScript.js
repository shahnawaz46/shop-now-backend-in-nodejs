import {
  migration,
  updateUserDetails,
  updateOrderModel,
  updatePublicId,
} from "./Migration.js";

export const allScript = () => {
  // migration();
  // updateUserDetails();
  // updateOrderModel();
  // updatePublicId();
};
